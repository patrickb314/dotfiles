# Models to keep pulled and served on localhost:11434.
#
# One model per line; blank lines and `#` comments are ignored. `script/setup`
# pulls anything in here that Ollama does not already hold. Nothing removes
# models, so dropping a line wants an `ollama rm` to go with it.

# Qwen for general chat and reasoning
qwen3.6:27b
# Zed's edit predictions
qwen3-coder:30b
# Embeddings for zotero-mcp semantic search
qwen3-embedding:8b
